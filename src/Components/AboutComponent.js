import React from 'react';

export default class About extends React.Component {
    render() {
        return(
            <div>
                <h1>This is About class , this will come once we give routing only</h1>
            </div>
        )
    }
}